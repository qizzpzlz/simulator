#pragma once

//enum TaskState { Idle, Running, Paused, Migrate, Swap, Unexecuted, Executed };
//
////enum JobState { Queued, Running, Finished, Canceled};

enum class NodeState { Idle, Running };

enum class TaskState { Unexecuted, Running, Completed };

